<%- partial('parts/firstimpression')%>
<section id="navedIndex" class="center-wrapper article-index">
  <% page.posts.each(function(post){ %>    
    <%- partial('parts/articlenutshell', {post: post}) %>
  <% }) %>
  <% if (page.total > 1){ %>
    <nav class="pagination">
        <%- paginator({
          prev_text: "&laquo;"+ __('prev'),
          next_text: __('next') +" &raquo;",
          format: 'page/%d/index.html#navedIndex'
        }) %>
    </nav>
  <% } %>
</section>
