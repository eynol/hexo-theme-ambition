<nav class="nav-header">
    <div class="logo-header">
        <a href="<%- url_for('/') %>" title="Jump to Homepage"><%= config.title %></a>
    </div>
    <div class="nav-mobile-avator" id="js-toggle-nav">
          <span></span>
    </div>
    <div class="nav-ul">
        <ul>
        <% 
        var menu ;
        if(config.menu){
            menu=config.menu
        }else{
            menu=theme.menu
        };
        for (var i in menu){ %>
        <li><a class="main-nav-link" title="Jump to <%= i %>" href="<%- url_for(menu[i]) %>"><%= i %></a></li>
        <% } %>
        <!-- <li><a class="main-nav-link close-btn"  href="javascript:;" onclick="open(location, '_self').close();">Exit</a></li>  this problem is not solved-->
        </ul>
    </div>
</nav>
